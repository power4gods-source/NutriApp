name: cookind
description: Cookind - Recetas, nutrición y cocina en comunidad
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Navigation
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  
  # State Management
  provider: ^6.1.1
  
  # HTTP & API
  http: ^1.1.2
  dio: ^5.4.0
  
  # Storage
  shared_preferences: ^2.2.2
  
  # Utils
  intl: ^0.20.2
  uuid: ^4.2.1
  crypto: ^3.0.3
  
  # Charts
  fl_chart: ^1.1.1
  
  # Supabase Auth (Google OAuth)
  supabase_flutter: ^2.8.0

  # Firebase (Spark) - Crashlytics, Storage, FCM
  firebase_core: ^3.8.1
  firebase_crashlytics: ^4.1.3
  firebase_messaging: ^15.0.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  
  # Image Picker
  image_picker: ^1.0.7
  
  # Auth: Google & Apple (opcional). Para activar: descomenta y ejecuta flutter pub get
  # google_sign_in: ^6.2.2
  # sign_in_with_apple: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    # - assets/fonts/  # Comentado hasta que se agreguen fuentes personalizadas
  
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont-Regular.ttf

